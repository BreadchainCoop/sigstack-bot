# DEPRECATED single-CVM compose.
# Use the two Phala product stacks instead:
#
#   docker/phala.transcription.yaml  — transcription CVM (4 GB)
#   docker/phala.translation.yaml    — translation CVM (4 GB)
#
# See docs/two-cvm-architecture.md

services: {}
