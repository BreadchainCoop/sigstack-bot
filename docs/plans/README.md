# Plans

Implementation-ready plans written by `/ce-plan` (Compound Engineering).

Product docs stay as siblings under [`docs/`](../). Do not put durable product specs here — use the named product markdown files instead.
