# Build artifacts
/target/

# Coverage reports (cargo-llvm-cov)
/lcov.info
**/lcov.info
/coverage/

# Node (commitlint / husky at repo root)
/node_modules/

# Environment files (NEVER commit these)
.env
.env.*
!.env.example
docker/.env
docker/*.env
!docker/*.env.example
!**/*.env.example
phala.env
**/secrets/

# Local Phala / dstack CLI state
**/.phala/

# Vercel link metadata (project/org IDs — do not share)
.vercel/

# Keys, certs, and local credential material
*.pem
*.key
*.p12
*.pfx
*.jks
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
credentials.json
**/service-account*.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Git worktrees
.worktrees/
