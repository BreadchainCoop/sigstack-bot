# Copy to site/.env (gitignored). Paste the token from !bot-username (second line)
# or startup logs (`username_token`). Never commit a real token.
# Site assembles https://signal.me/#eu/<token> — do not put a full URL here.

PUBLIC_SIGNAL_USERNAME_TOKEN=YOUR_USERNAME_TOKEN_HERE
