# DEPRECATED — do not deploy.
#
# Local Whisper on a dedicated transcription CVM is not a production scale path
# (see docs/solutions/architecture-patterns/2026-08-13-cpu-tee-whisper-does-not-scale.md).
# Both Signal products now run on one CVM:
#
#   docker/phala.translation.yaml
#   docker/phala.translation.env
#
# Upgrade in place: phala deploy --cvm-id <existing translation CVM> …

services: {}
