{
  "name": "sigstack-bot",
  "private": true,
  "description": "Dev tooling for commitlint / husky (Rust app lives in crates/)",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit",
    "commitlint:last": "commitlint --last --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  }
}
